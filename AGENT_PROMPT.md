# פרומפט לסוכן CLI — פריסת "מד הבחירות" חי, ב־0 ש"ח

העתק את כל מה שמתחת לקו לסוכן (Claude Code וכד'), כשאתה נמצא בתיקייה
שמכילה את `election-meter/` (הקבצים המצורפים).

---

אתה סוכן CLI. המשימה: לקחת מערכת חיזוי בחירות **קיימת, בדוקה ועובדת**
שנמצאת בתיקייה `./election-meter/` ולהפוך אותה לאתר חי שמתעדכן אוטומטית
כל ערב — בעלות 0 ש"ח, ללא שרתים, ללא API בתשלום. GitHub free tier בלבד.

## מה כבר קיים (אל תכתוב מחדש!)
- `scrape_polls.py` — גירוד סקרי מנדטים מוויקיפדיה האנגלית → `polls.csv`
- `bader_ofer.py` — מנוע חלוקת מנדטים (חסימה 3.25% + הסכמי עודפים).
  **מאומת 1:1 מול תוצאות 2022 הרשמיות. אסור לשנות את הלוגיקה שלו.**
- `model.py` — אגרגציה מכוילת (half-life=4, תיקון מגזרי ×0.3,
  SYSTEMATIC_SD=0.012 — פרמטרים שנקבעו ב־backtest על 2021–2022) +
  20K סימולציות מונטה קרלו → `forecast.json`
- `index.html` — דשבורד RTL בעברית שקורא `forecast.json` (סטטי לחלוטין)
- `backtest.py`, `sector_bias.py` — סקריפטי ולידציה
- `README.md`, `BACKTEST_REPORT.md` — תיעוד

## חוקי ברזל
1. **0 ש"ח**: אין שרת, אין DB, אין מפתחות API. רק GitHub Actions + Pages.
2. **אל תשכתב לוגיקה קיימת.** מותר לתקן באגים קטנים רק אם מבחן הקבלה נכשל,
   ובתנאי ש־`python bader_ofer.py` ממשיך לעבור.
3. **עמידות**: אם הגירוד נכשל (וויקיפדיה שינתה מבנה) — ה־workflow חייב
   להשאיר את `forecast.json` האחרון התקין באוויר, לא לשבור את האתר.
4. גירוד מנומס: User-Agent מזוהה, בקשה אחת ליום, בלי לולאות retry אגרסיביות.

## שלבי ביצוע
1. `cd election-meter && pip install requests pandas lxml beautifulsoup4 numpy`
2. הרץ מבחני קבלה מקומיים (סעיף "מבחני קבלה" למטה). אל תמשיך אם נכשלו.
3. אתחל ריפו git, צור ריפו ציבורי ב־GitHub בשם `election-meter`
   (עם `gh repo create` אם gh מותקן; אחרת הנחה אותי לעשות זאת ידנית), ודחוף.
4. צור `.github/workflows/daily.yml`:
   - cron: `"0 16 * * *"` (19:00 שעון ישראל בקיץ) + `workflow_dispatch`
   - שלבים: checkout → setup-python 3.12 → pip install →
     `python scrape_polls.py && python model.py` →
     **אם ורק אם** שני הסקריפטים הצליחו ו־`forecast.json` תקין (JSON נטען
     ומכיל ≥8 מפלגות): commit+push של `polls.csv` + `forecast.json`.
     אחרת: צא בשגיאה בלי לדחוף כלום (האתר נשאר עם הנתונים הקודמים).
5. הפעל GitHub Pages מ־branch main, root (דרך `gh api` או הנחה אותי).
6. הרץ את ה־workflow ידנית (`gh workflow run`), חכה שיסתיים ירוק,
   ואמת שהאתר החי מציג נתונים (curl ל־URL של Pages ובדוק שה־HTML נטען
   ו־`forecast.json` נגיש).
7. הוסף ל־README את ה־URL החי ואת פקודת ההרצה הידנית.

## מבחני קבלה (חובה, הרץ ודווח על כולם)
- [ ] `python bader_ofer.py` — יוצא בקוד 0 ומדפיס "Matches the official"
- [ ] `python scrape_polls.py` — יוצר `polls.csv` עם ≥100 שורות,
      והתאריך המקסימלי בו הוא מ־90 הימים האחרונים
- [ ] `python model.py` — יוצר `forecast.json` תקין: JSON נטען, מכיל
      `parties` (≥8 פריטים), `blocs` עם `p_61` בין 0 ל־1,
      וסכום `expected_seats` של כל המפלגות בין 115 ל־125
- [ ] `python backtest.py` — רץ עד הסוף בלי חריגות (לוקח דקות ספורות)
- [ ] ה־workflow רץ ירוק ב־GitHub לפחות פעם אחת
- [ ] האתר החי עונה 200 ומציג את הדשבורד עם מספרים

## מה מותר לשפר (אופציונלי, רק אחרי שהכול ירוק)
- כפתור/טקסט "עודכן לאחרונה" בולט בדשבורד
- שמירת היסטוריה: append יומי של שורת סיכום ל־`history.csv`
  (תאריך, מנדטים לכל גוש, p_61) — לגרף מגמה עתידי
- קובץ `daily_post.md` שנוצר בכל ריצה: תקציר בעברית מוכן להדבקה ידנית
  באתר שלי (טבלת מפלגות, גושים, מפלגות בסיכון חסימה, תאריך)

## דיווח סופי
בסוף החזר לי: URL האתר החי, URL הריפו, סטטוס כל מבחני הקבלה,
ומה לעשות אם ריצה לילית נכשלת.
